import FlyBehavior from "./FlyBehavior";

class FlyRocketPowerBehavior extends FlyBehavior {
  fly(): void {
    console.log("Flying like a rocket! 🚀");
  }
}

export default FlyRocketPowerBehavior;
